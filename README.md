# Wecovi WebStorm Plugin

WebStorm 2024.1 이상을 대상으로 하는 IntelliJ Platform 플러그인입니다.

## 시작

JDK 17이 필요합니다. (WebStorm 2024.1 런타임 호환성)

```bash
./gradlew runIde
```

배포 ZIP은 다음 명령으로 만듭니다.

```bash
./gradlew buildPlugin
```

생성물은 `build/distributions/`에 위치합니다.
